shamefully-hoist=false
strict-peer-dependencies=false
auto-install-peers=true

# Scope → GitHub Packages. Auth: set NODE_AUTH_TOKEN (PAT with read/write:packages)
# or let actions/setup-node write //npm.pkg.github.com/:_authToken=... in CI.
@JasonTuTu2:registry=https://npm.pkg.github.com
