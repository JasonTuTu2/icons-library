{
  "name": "@JasonTuTu2/icons-figma",
  "version": "0.0.0",
  "private": true,
  "description": "Figma plugin — export GenVoice custom icons to the icon browser",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "echo \"figma plugin has no unit tests\"",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.109.0",
    "esbuild": "^0.24.2",
    "rimraf": "^6.0.1",
    "typescript": "^5.7.2"
  }
}
