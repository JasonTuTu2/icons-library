{
  "name": "@JasonTuTu2/icons-figma",
  "version": "0.0.0",
  "private": true,
  "description": "Figma plugin — stage GenVoice custom icons and dispatch Apply/Publish",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "echo \"figma plugin has no unit tests\"",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@JasonTuTu2/github-admin": "workspace:*"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.109.0",
    "esbuild": "^0.24.2",
    "rimraf": "^6.0.1",
    "typescript": "^5.7.2"
  }
}
